<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">
  <modelVersion>4.0.0</modelVersion>
  <groupId>com.biblio</groupId>
  <artifactId>biblio-municipale</artifactId>
  <name>GestionBibliothequeMunicipale</name>
  <version>1.0.0</version>
  <build>
    <finalName>biblio-municipale</finalName>
    <plugins>
      <plugin>
        <artifactId>maven-compiler-plugin</artifactId>
        <version>3.11.0</version>
        <configuration>
          <release>${maven.compiler.release}</release>
        </configuration>
      </plugin>
      <plugin>
        <groupId>org.openjfx</groupId>
        <artifactId>javafx-maven-plugin</artifactId>
        <version>0.0.8</version>
        <configuration>
          <mainClass>com.biblio.app.MainApp</mainClass>
        </configuration>
      </plugin>
      <plugin>
        <artifactId>maven-shade-plugin</artifactId>
        <version>3.5.0</version>
        <executions>
          <execution>
            <phase>package</phase>
            <goals>
              <goal>shade</goal>
            </goals>
            <configuration>
              <outputFile>${project.build.directory}/fat-jar/biblio-municipale.jar</outputFile>
              <transformers>
                <transformer>
                  <mainClass>${main.class}</mainClass>
                </transformer>
                <transformer />
              </transformers>
              <filters>
                <filter>
                  <artifact>*:*</artifact>
                  <excludes>
                    <exclude>META-INF/*.SF</exclude>
                    <exclude>META-INF/*.DSA</exclude>
                    <exclude>META-INF/*.RSA</exclude>
                  </excludes>
                </filter>
              </filters>
            </configuration>
          </execution>
        </executions>
      </plugin>
      <plugin>
        <groupId>org.panteleyev</groupId>
        <artifactId>jpackage-maven-plugin</artifactId>
        <version>1.6.1</version>
        <executions>
          <execution>
            <id>jpackage</id>
            <phase>install</phase>
            <goals>
              <goal>jpackage</goal>
            </goals>
            <configuration>
              <name>GestionBibliotheque</name>
              <type>EXE</type>
              <vendor>Municipalite</vendor>
              <appVersion>${project.version}</appVersion>
              <destination>${project.build.directory}/installer</destination>
              <input>${project.build.directory}/fat-jar</input>
              <mainJar>biblio-municipale.jar</mainJar>
              <mainClass>${main.class}</mainClass>
              <winConsole>true</winConsole>
            </configuration>
          </execution>
        </executions>
      </plugin>
    </plugins>
  </build>
  <properties>
    <maven.compiler.release>19</maven.compiler.release>
    <hibernate.version>6.4.2.Final</hibernate.version>
    <main.class>com.biblio.app.Launcher</main.class>
    <slf4j.version>2.0.12</slf4j.version>
    <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
    <javafx.version>19.0.2.1</javafx.version>
    <logback.version>1.4.14</logback.version>
  </properties>
</project>
